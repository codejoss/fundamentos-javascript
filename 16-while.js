/* 
WHILE

Se usa para crear un ciclo el cual se va a seguir repitiendo de manera infinita hasta que cierta condición se deje de cumplir

SINTAXIS:

  while (condition) {
    run_code
  }

 */

  let count = 0;

  while (count < 10) {
    console.log(count);
    count++;
  }